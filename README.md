# SkillStack (PromptStack attachment)

This is an **agent-first** repository: a PromptStack attachment (this tree loaded by a harness such as OpenClaw, Hermes, Cursor, or Grokbot). It is not a named person or product-bot.

Human background notes (what this pack is for, where to put your own purposes, name disambiguation) are at the end: [Human background notes](#human-background-notes).

This is a filtered SkillStack Kernel snapshot. Clone this tree; do not fetch other repos without operator auth.

**Lineage:**
- Canonical public repo: https://github.com/graphedge/SkillStack
- This tree is the allowed PromptStack corpus when cloned from that repo.
- Private operator source (SkillStack2) is not published here; do not fetch it unless the operator authorizes.
- Kernel operator id: grokbot-soul (filter id only; not a second public taxonomy).

**Outside-repo rule:** Never pull information from repositories outside this snapshot unless you have asked the operator and they have authorized it.

**Precedence (MUST, high to low):**
1. Operator authorization (including the outside-repo rule)
2. Files that exist in this snapshot
3. Fast path for routine local edits
4. Board workflow and stratification protocol for architecture
5. Customer fill-ins, flows/samples/, and sumplan/ are examples only (not law)

**Missing-hop gate (MUST):** A name in an index is not a file. If the path is absent, stop. Do not invent the missing skill. Do not fetch another repository. Ask the operator.

**Sample gate (MUST):** Customer fill-in sections are NON-CANON shape examples. MUST NOT treat them as this snapshot's law.

**Fast path (MUST):** For a local tweak (one file, no new skill, no DNA change): edit the file and state a one-line verdict. MUST NOT run the Board sequence or the full proposal template.

- Kernel ID: grokbot-soul
- Minimum security level: PUBLIC
- Included entities: 40
- Excluded entities: 73

Membership was derived from the entity registry and Kernel filter rules.
Section-level security overrides were applied to Markdown files.

## Human background notes

This snapshot is a **framework for skill adoption**, not a finished product agent. Folders are slots: put your own purposes in the appropriate places (Board, writing, project-classes, and the NON-CANON fill-in hops). Replace fill-ins with your rules. If a named hop is absent, do not invent it; use fast path for a local edit or ask the operator.

**Not these other SkillStacks.** This is not the Claude Code plugin catalog at https://github.com/viktorbezdek/skillstack and not the Ireland e-learning product at skillstack.ie. PromptStack here means this tree attached to a harness, not the GitHub projects named PromptStack.
