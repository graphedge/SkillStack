## [PUBLIC] Customer fill-in

**Invoke (example only — not a skill):** `-ex @mode-selection`

**NON-CANON.** Example of what a customer would put in this slot. Replace with your own rules. MUST NOT treat this sample as Board or writing canon.

Example (a SaaS repo):
- `-ex` — change the named app or docs file only.
- `-mod` — change agent rules (`AGENTS.md`, Board files) only when the operator asked.
- No prefix — treat as `-ex`.
If a needed file is not in this snapshot, stop and ask the operator.

