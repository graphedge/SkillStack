## [PUBLIC] Customer fill-in

**Invoke (example only — not a skill):** `-ex @alignment-verification`

**NON-CANON.** Example of what a customer would put in this slot. Replace with your own rules. MUST NOT treat this sample as Board or writing canon.

Example: write a `placement-record` — does this change agent rules or only one app file? If agent rules, it is `-mod`. Roleplay without that artifact does not count.

