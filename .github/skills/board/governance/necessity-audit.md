## [PUBLIC] Customer fill-in

**Invoke (example only — not a skill):** `-ex @necessity-audit`

**NON-CANON.** Example of what a customer would put in this slot. Replace with your own rules. MUST NOT treat this sample as Board or writing canon.

Example: write a `veto-record` if an existing file already does the job. Do not add a twin. Fast-path tweaks skip this slot.

