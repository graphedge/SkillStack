## [PUBLIC] Customer fill-in

**Invoke (example only — not a skill):** `-ex @recursive-decomposition`

**NON-CANON.** Example of what a customer would put in this slot. Replace with your own rules. MUST NOT treat this sample as Board or writing canon.

Example (a docs site):
- Split a skill if it would exceed ~200 lines or two jobs.
- At most three children. Write a text tree: parent → children.
- Do not add a folder just to match a sibling.

