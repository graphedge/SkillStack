## [PUBLIC] Customer fill-in

**Invoke (example only — not a skill):** `-ex @manifest-generation`

**NON-CANON.** Example of what a customer would put in this slot. Replace with your own rules. MUST NOT treat this sample as Board or writing canon.

Example (a docs site):
- New skill file: title, invoke, inputs, outputs, owner, one-line why.
- Name files that must exist. If a dependency is not in this snapshot, stop.
- Fast-path tweaks do not use this slot.

